# 🚀 راهنمای سریع افزودن مقاله جدید

## ✨ خبر خوب!
**دیگر نیازی به بروزرسانی دستی فایل‌های جاوااسکریپت نیست!**

سیستم جدید به صورت خودکار تمام مقالات را پیدا و نمایش می‌دهد.

---

## 📝 برای افزودن مقاله جدید، فقط ۳ مرحله:

### ۱. کپی کردن تمپلیت
```bash
# فایل تمپلیت را کپی کنید
cp article-template.html article-6.html
```
> **نکته:** شماره فایل جدید باید بزرگتر از آخرین شماره موجود باشد (۱، ۲، ۳، ... تا ۵۰)

### ۲. ویرایش اطلاعات مقاله (متادیتا)
در تگ `<article>` در ابتدای فایل، موارد زیر را تغییر دهید:

```html
<article 
    data-id="6"                                          ← شماره مقاله
    data-category="شبکه و سیاستگذاری"                   ← دسته‌بندی
    data-author="تیم فیلتربین" 
    data-date="۲۰ آبان ۱۴۰۳"                             ← تاریخ به فارسی
    data-reading-time="15"                               ← زمان مطالعه (دقیقه)
    data-excerpt="خلاصه کوتاه مقاله..."                  ← توضیح کوتاه (۱۰۰-۱۵۰ کاراکتر)
    data-tags="تگ۱,تگ۲,تگ۳"                              ← برچسب‌ها (با کاما جدا شوند)
    data-featured="true">                                ← مقاله ویژه (true/false)
```

### ۳. نوشتن محتوای مقاله
- عنوان اصلی را در `<h1 class="article-body-title">` بنویسید
- تصویر کاور را در `<img class="article-image">` قرار دهید
- محتوای مقاله را بنویسید

---

## ✅ تمام! همین!

**سیستم خودکار:**
- ✅ فایل جدید را پیدا می‌کند
- ✅ اطلاعات را از متادیتا می‌خواند
- ✅ مقاله را در صفحه یادداشت‌ها نمایش می‌دهد
- ✅ بر اساس شماره ID مرتب می‌کند (جدیدترین اول)

---

## 🎨 افکت شیشه‌ای (Glassmorphism)

مقالات با دسته‌بندی **"شبکه و سیاستگذاری"** به صورت خودکار افکت شیشه‌ای زیبا دریافت می‌کنند!

برای افزودن دسته‌بندی‌های دیگر به افکت شیشه‌ای:
1. فایل `js/components/articles-list.js` را باز کنید
2. در متد `getSpecialCategoryClass` دسته‌بندی دلخواه را اضافه کنید:

```javascript
getSpecialCategoryClass(category) {
    const glassCategories = [
        'شبکه و سیاستگذاری',
        'دسته‌بندی جدید'  // ← اضافه کنید
    ];
    
    if (glassCategories.includes(category)) {
        return 'glass-effect';
    }
    return '';
}
```

---

## 📂 دسته‌بندی‌های پیشنهادی

```
✅ تکنولوژی
✅ امنیت
✅ سانسور
✅ شبکه و سیاستگذاری (با افکت شیشه‌ای)
✅ حریم خصوصی
✅ ابزار دیجیتال
```

---

## 🔧 تنظیمات پیشرفته (اختیاری)

### استفاده از فایل JSON برای مدیریت دستی

اگر می‌خواهید کنترل بیشتری داشته باشید، می‌توانید از فایل `articles.json` استفاده کنید:

```json
{
  "articles": [
    {
      "id": "6",
      "file": "article-6.html",
      "title": "عنوان مقاله",
      "category": "دسته‌بندی",
      "featured": true,
      "active": true
    }
  ]
}
```

**مزایا:**
- ✅ غیرفعال کردن مقاله بدون حذف فایل (`active: false`)
- ✅ کنترل دقیق‌تر روی ترتیب نمایش
- ✅ افزودن متادیتای بیشتر

**نکته:** اگر فایل JSON وجود نداشته باشد، سیستم خودکار کار می‌کند!

---

## 💡 نکات مهم

### ✅ انجام دهید:
- شماره‌های پی‌درپی استفاده کنید (۱، ۲، ۳، ...)
- خلاصه مقاله را جذاب و خلاصه بنویسید
- از تصاویر بهینه شده استفاده کنید (حداکثر 500KB)

### ❌ انجام ندهید:
- شماره‌های تکراری استفاده نکنید
- فایل `article-template.html` را تغییر ندهید (برای کپی است)
- فراموش نکنید متادیتای `data-*` را پر کنید

---

## 🐛 رفع مشکل

### مقاله جدید نمایش داده نمی‌شود؟

1. **کنسول مرورگر را باز کنید** (F12)
2. پیام `✅ تعداد X مقاله یافت شد` را بررسی کنید
3. بررسی کنید:
   - ✅ فایل با نام `article-N.html` است (نه `article-N copy.html`)
   - ✅ متادیتای `data-id` درست است
   - ✅ فایل در پوشه `data/articles/` است
   - ✅ کش مرورگر را پاک کنید (Ctrl+Shift+R)

### استایل‌ها درست نیست؟

- مرورگر را Refresh کنید (Ctrl+F5)
- مسیر فایل‌های CSS را بررسی کنید

---

## 📞 پشتیبانی

- **ایمیل:** support@abzarestan.ir
- **تلگرام:** @abzarestan
- **راهنمای کامل:** `README.md` و `HOW-TO-ADD-ARTICLES.md`

---

## 🎉 پایان!

حالا می‌توانید به راحتی مقالات جدید اضافه کنید بدون اینکه نگران بروزرسانی کدهای جاوااسکریپت باشید!

**نسخه:** 2.0 (با Auto-Discovery)  
**آخرین بروزرسانی:** ۱۶ آبان ۱۴۰۴  
**نویسنده:** تیم فیلتربین
